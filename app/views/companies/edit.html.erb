<div class="container">

	<h1 class="display-4 mt-4 text-center">Edit Your Company Profile</h1>

	<%= form_with scope: :company, model: @company, local: true do |form| %>
		<div class="form-group mt-5" style="width: 50%; margin: 0 auto">

		  <p>
		    <%= form.label :name %>
		    <%= form.text_field :name, {:class => "form-control", :minlength => 3, :maxlength => 50, :required => "true"} %>
		    <small class="form-text text-muted">3 to 50 characters</small>
		  </p>

		  <p>
		    <%= form.label :display_picture %><br>
		    <%= form.url_field :display_picture, {:class => "form-control"} %>
		  </p>

		  <%= link_to 'Back',company_path, {:class => "btn btn-outline-secondary"} %>
		  <%= form.submit "Save", class: "btn btn-primary" %>

		</div>
	<% end %>

</div>